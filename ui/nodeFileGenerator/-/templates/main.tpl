<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="path">
        <div class="module_path">
            {MODULE_PATH}
        </div>
        <div class="node_path">
            <input type="text" value="{NODE_PATH}">
        </div>
    </div>

    <div class="types">
        <!-- type -->
        <!-- if type/template -->
        <div class="name">{NAME}</div>
        <div class="buttons">

            <!-- type/template -->
            {BUTTON}
            <!-- / -->

            <div class="cb"></div>
        </div>
        <!-- / -->
        <!-- / -->
    </div>

</div>
